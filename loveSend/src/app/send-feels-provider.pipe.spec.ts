import { SendFeelsProviderPipe } from './send-feels-provider.pipe';

describe('SendFeelsProviderPipe', () => {
  it('create an instance', () => {
    const pipe = new SendFeelsProviderPipe();
    expect(pipe).toBeTruthy();
  });
});
